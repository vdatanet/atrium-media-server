# Audit — 2026-08-30

The full `/audit` of the whole repository at `main` @ `71b0bcc`, run 2026-08-30 immediately after
011 T3 merged: provenance (family A), the documents against reality (B), tests that cannot fail
(C), zero delta (D) and hygiene (E). Line numbers refer to the audited tree and are not maintained
as fixes land — each finding's quote is the stable locator.

**Read this file the way `tasks.md` is read.** An unticked box is an open debt. The pull request
that resolves a finding ticks its box **in the same commit**, so the record and the repository
never disagree about what is still open. A finding that turns out to be wrong is not deleted: it
is ticked with a one-line note saying why it was wrong, because a refuted finding is a calibration
datum for the next audit.

**Nothing in this audit is a bug in the code.** `ruff · mypy · pytest` is green on every finding
below, which is the reason they need a person: each one is a claim a document makes that the
repository no longer supports, and no gate in this project can see that class of drift.

**Not run, and therefore not asserted here:**

- **A1 paragraph by paragraph** across all thirteen features — run as a grep heuristic over
  reference-behaviour phrasings, with the survivors read by hand. The older features were sampled,
  not swept.
- **B2 exhaustively** — `decide` and `negotiate_range` were read against their contract lines
  (both clean) and so was 008's `hls` block, which is M1. The other plans' §5 blocks were not read
  line by line.
- **B3 row by row** — the audit of 2026-08-28 did that sweep and closed its findings in its own
  file. Only 008 T15's addition was checked here; it has a test.
- **C1 beyond the golden-drift check** CI already runs (`git diff --exit-code -- tests/golden`).
- **D2 and D3 by hand** — the property names are held by the `test_aliases` and `test_shapes`
  sweeps instead.
- **Anything needing a live Jellyfin.** This audit ran read-only and started no probe, which is
  why L6 is the one finding it cannot close from here.

**Findings: 0 high, 2 medium, 7 low.**

## How these close

| Pull request | Findings |
|---|---|
| 1 — this record | — |
| 2 — the 008 plan's HLS contract block | M1 |
| 3 — document mends needing no measurement and no code | L1, L2, L4, L5, L7 |
| 4 — citations needing the Jellyfin checkout | M2, L3 |
| a measurement session against the reference, when one is next open | L6 |

The measurement-session finding stays open in this file until a probe run discharges it; that is
what this file is for.

---

## Medium

- [x] **M1 · `specs/008-playback-negotiation-and-delivery/plan.md:290–296`** — the §5 contract
  block for `media/hls.py` has drifted from the code in all three signatures, and its trailing
  comment asserts as a property exactly what 008 T15 measured as false.
  > `def master_playlist(decision, stream_facts, main_query: str) -> str   # exactly one variant`

  T15 merged as `cab9443` on 2026-08-30, amending `spec.md`, `tasks.md`, `behaviours.md` and the
  code — but not this plan, whose `amended:` frontmatter still stops at T14. The same clause
  survives a second time at `plan.md:529`:
  > "with exactly one variant there is nothing for a client to select on it"

  Read off `src/atrium/media/hls.py` at `71b0bcc`, the three real signatures are
  `plan_segments(runtime_ticks: int, milliseconds: int, keyframes: Sequence[int] | None = None) ->
  tuple[Segment, ...]`, `media_playlist(segments: Sequence[Segment], *, query: str, container: str
  | None) -> str` and a keyword-only `master_playlist` taking `query`, `video`, `audio`,
  `source_video`, `frame_rate` and `options`. Not one parameter list in the block survives, and
  `plan_segments` returns a tuple where the block promises a list.

  This is the finding with a cost attached: 011 T11 builds the manifest, and the contract its
  implementer will open is this block. It is the same sentence 011's own tasks gate caught in a
  different document and called its sharpest finding — true when written, false hours later. The
  008 plan is the last place it survives. Breaches Principle III (documentation moves with the
  code, in the same commit). Resolve: replace the three signatures, correct the §6.4 clause, and
  add the T15 amendment line the frontmatter never got.

- [ ] **M2 · `specs/001-server-identity-and-discovery/tasks.md:554`** — an observable reference
  behaviour with no citation and no marker, load-bearing because it is the justification offered
  for a surface decision under Principle VI.
  > "The reference serves its OpenAPI document at `/api-docs/openapi.json`; that route is not in
  > `surface.yaml` and no analysed client asks for it"

  Breaches Principle II. Resolve: a `[source: … @ v10.11.11]` naming the file that maps that
  route, or the `⚠️ UNVERIFIED` marker. Needs the Jellyfin checkout.

## Low

- [x] **L1 · `specs/008-playback-negotiation-and-delivery/tasks.md:416`** — the one Markdown
  anchor in the repository that does not resolve.
  > `[002 §3.5](../002-authentication-users-and-sessions/spec.md#35-get-usersuserid--getuserbyid)`

  002 §3.5 is *"The user object"* (`spec.md:224`); its anchor is `#35-the-user-object`, and the
  heading this link names has not existed since the section was renamed. Resolve: correct the
  anchor.

- [x] **L2 · `specs/001-server-identity-and-discovery/plan.md:242`** — a reference behaviour
  restated with neither citation nor pointer.
  > "The reference emits .NET round-trip format: seven fractional digits and a `Z`"

  Principle II. The measurement exists — behaviours §1.2 carries `[prior-probe: Jellyfin 10.11.11,
  2026-06-19]` — so a pointer to it discharges this and no new measurement is owed. Resolve: cite
  the behaviours section, in the form the plan's own preamble promises ("where this plan states a
  reference behaviour, the citation lives in the spec section it names").

- [ ] **L3 · `specs/002-authentication-users-and-sessions/plan.md:442`** — a claim about the
  reference's storage, uncited, in a rejected-alternatives paragraph.
  > "**Store tokens in plain text, as the reference does.**"

  Principle II. The decision it justifies stands without the subordinate clause. Resolve: a
  `[source:]` citation, or drop the clause. Needs the Jellyfin checkout.

- [x] **L4 · `AGENTS.md:26`** — a state sentence that is already false, in the document whose own
  next paragraph explains why it should not contain one.
  > "so **011 is ready for code, starting at its T1**"

  011 T1–T3 are merged (pull requests #194, #195, #196). The same document says *"the state is in
  the files, not here, so it cannot go stale"* and its table already answers this question with
  "the first unticked task". Principle III. Resolve: reword to the form that cannot go stale.

- [x] **L5 · `specs/011-subtitle-delivery/spec.md:542`** — a source claim whose citation lives one
  document away, in the direction this project's convention runs against.
  > "the item's own internal metadata directory, where the reference puts a subtitle it downloaded
  > or extracted"

  The citation exists at `plan.md:865` — `[source:
  MediaBrowser.Providers/MediaInfo/MediaInfoResolver.cs:216-226 @ v10.11.11]` — but the spec holds
  the citation and the plan points at it, not the other way round. **Flagged uncertain**: §3.6's
  claim is arguably covered by the broader `MediaInfoResolver.cs:85-253` citation the same spec
  carries at `:821`, in which case this is a preference and not a breach. Resolve: copy the
  narrower citation into §3.6, or tick this with the reason it was wrong.

  **Judged real.** `:821` is inside §8, *References* — a bibliography of every source the feature
  read, not an inline citation of this claim. Principle II asks for provenance *inline*, so a
  bibliography entry does not discharge a §3 sentence. The narrower citation is now in §3.6.

- [ ] **L6 · `docs/compatibility/client-embeat-mobile.md:482`** — a claim about the pinned OpenAPI
  document written without the `[spec:]` form that verifies it.
  > "the reference does not declare it there"

  The claim decides a delta: it is the reason §6.2 refuses a client-supplied `PlaySessionId` on
  `/universal`. Resolve: `[spec: GetUniversalAudioStream]`, checked against
  `reference/openapi.json` — which is fetched and never vendored, so this needs
  `tools/fetch_reference_spec.py` against a live server. **Flagged uncertain**: the client trace
  documents carry their own citation conventions and this may be within them. Needs a measurement
  session.

- [x] **L7 · `specs/005-item-query-api/notes/item-shapes.md:109`** — a relative date, in a
  paragraph explaining why something is unmeasured.
  > "this repository has no `reference/` checkout at the moment"

  It becomes false in silence the day somebody fetches one, which is the failure mode the
  absolute-dates rule exists for (AGENTS.md, *"Dates are absolute"*). Resolve: anchor it to the
  date it was written.

---

## Checked and clean

Counted at `71b0bcc` on 2026-08-30, by this audit rather than carried from the last one:

**A2** — 38 of the 40 distinct `[probe: tools/*.py]` names cited anywhere in the tree resolve to a
script on disk. The two that do not are `tools/probe_x.py` and `tools/x.py`, both of which appear
only as the documented *placeholder* in AGENTS.md, `tools/README.md`, `specs/010`'s spec and the
audit command itself. All 243 `@ v` tags in the repository read `v10.11.11`, matching the pin; the
only two other version mentions are `10.11.x` prose about the pin's scope.

**A3** — every `prior-probe` date (2026-06-13 ×18, 2026-06-19 ×1, 2026-08-14 ×2) precedes this
repository's first commit. No citation has been copied off a neighbour and re-dated.

**A4** — no `⚠️ UNVERIFIED` marker survives in any spec that has left draft. Checked across all
twelve `spec.md` files: eight `Implemented`, two `Accepted`, two `Draft`, zero markers in any of
them.

**A5** — every one of the 66 behaviour entries carries its three required fields (66 **Jellyfin
does:**, 66 **Depends on it:**, 66 **Atrium does:**; the three further "Atrium does:" occurrences
are §-body prose, not entry headers).

**A6** — no private path and no local absolute path appears in any citation.

**B1** — every module a plan's §5 draws resolves against `src/`. 011's four absent modules are
T5–T11's, by design.

**B4** — frontmatter `status:`, the gate fields and the `specs/README.md` table agree across all
thirteen features. L4 is the only stale status *sentence* in the repository.

**B5** — every ticked task carries a Done note, in each of the three formats the feature
generations use (`### Done —`, `- **Done (date):**`, `**Done (date).**`). 011 is 3 tasks, 3 notes.

**C2** — no tautological or empty assertion found.

**C3** — every `xfail` is `strict=True` and has a structural test of its own; every `skip` is
conditional on a real absence.

**C4** — sampled: the network guard, the hostile-path check and the route surface each have a test
that fails when the guard is removed.

**C5** — sampled over `FEATURE_008`; the acceptance map's own three failure modes are asserted.

**D1** — enforced by the suite itself (`tests/conformance/test_routes.py:123`,
`test_no_route_ships_ahead_of_its_feature`), green.

**E1** — SPDX on all 310 tracked `.py` files.

**E2** — no non-English commit subject on `main`.

**E3** — `.env` untracked and `reference/` uncommitted.

**E4** — every commit on `main` traces to a numbered pull request: 55 two-parent merges, 138
squash merges carrying `(#N)`, and the root commit. 194 first-parent commits, one of them the
scaffolding.
