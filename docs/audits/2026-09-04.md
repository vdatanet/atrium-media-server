# Audit — 2026-09-04

The full `/audit` of the whole repository at `main` @ `0f38efc`, run 2026-09-04 immediately after
012 T11 merged and the last unbuilt feature was marked `Implemented`: provenance (family A), the
documents against reality (B), tests that cannot fail (C), zero delta (D) and hygiene (E). Line
numbers refer to the audited tree and are not maintained as fixes land — each finding's quote is
the stable locator.

**Read this file the way `tasks.md` is read.** An unticked box is an open debt. The pull request
that resolves a finding ticks its box **in the same commit**, so the record and the repository
never disagree about what is still open. A finding that turns out to be wrong is not deleted: it is
ticked with a one-line note saying why it was wrong, because a refuted finding is a calibration
datum for the next audit.

**One finding is a possible delta and the rest are not bugs.** `ruff · mypy · pytest` is green on
every finding below, which is the reason they need a person. H1 is the exception in kind: if its
row is accurate then the code is wrong rather than the document, and that is settled by a
measurement rather than by reading.

**Findings: 3 high, 18 medium, 16 low.**

## The audit's own calibration

Three things about this run the next audit should be able to see:

* **B2 and B3 were each run twice over overlapping ranges, and neither pass found what the other
  did.** The full-repository B2 found H3 and M11; the 007–012 pass found L9–L14, and called 009
  clean where the full pass found H3 in it — both had read `by_id_for_deletion`, and they differed
  on whether §6 mentioning a method excuses §5 denying it exists. The consolidated count is
  therefore higher than either pass reported. **A single pass over this repository under-reports.**
* **The first consolidated summary said "fourteen" for the missing-criterion class.** It is ten
  findings covering fourteen rows. The count is corrected here rather than quietly restated,
  because the task list below is built from it.
* **Family C's most valuable finding came from running a mutation, not from reading.** M17 was
  found by deleting a guard on a scratchpad copy and watching ~5250 tests still pass. No amount of
  reading would have produced it, and the project document that describes that guard claims the
  opposite.

## The corrective plan

Ten tasks, one branch and one pull request each, in the order below. **C1 is first because it is
the only finding that might be a delta**, and because what it measures decides whether 001's spec
or 001's code is the thing that changes.

| # | Task | Closes | Depends on |
|---|---|---|---|
| C1 | Measure whether the reference refuses `/System/Info` without permission, then take the decision it forces | H1 | — |
| C2 | The guard nothing holds, and the assertion that cannot fail | M17, M18 | — |
| C3 | Two contracts that say something false about the code | H2, H3 | — |
| C4 | 005's three uncovered tables get their criteria | M4, M5, M6 | — |
| C5 | 008, 009 and 011's refusal tables get theirs — and AC-32 learns to count | M7, M8, M9, M10 | — |
| C6 | 001, 002 and 004's uncovered rows get theirs | M1, M2, M3 | — |
| C7 | The stale contracts and the undrawn modules | M11, M12, L2, L7–L14 | — |
| C8 | Provenance: one discharged citation, one unrunnable one, two uncited claims | M13, L4, L5, L6 | — |
| C9 | Claims the repository no longer supports | M14, M15, M16, L1, L3 | — |
| C10 | The acceptance map's half-named criterion, and the golden anchor no test reads | L15, L16 | — |

**C4, C5 and C6 are one job split by feature so each review is coherent**, not three different
jobs. Whoever takes the first of them should read M1 through M10 together — they are a single class, and the reason they exist is that the acceptance map
checks only that a named test *exists*, never that a behaviour *has* a name.

**C1 needs its owner before it needs a branch.** Measure first; the three outcomes are a divergence
entry, a criterion plus an implementation, or a corrected spec row, and they are not the task's to
choose.

## How these close

| Pull request | Findings |
|---|---|
| this record | — |

Nothing in this record is discharged yet.

---

## High

- [ ] **H1 · `specs/001-server-identity-and-discovery/spec.md:163`** — a specified refusal that
  nothing implements, no criterion covers, and which may be a delta.
  > "| Valid token without permission | `403` |"

  `get_system_info` (`src/atrium/api/system.py:130-137`) gates on plain `require_user`: there is no
  permission check, no `403` path and no test. AC-5 says `/System/Info` answers "`200` with a valid
  one", which reads as the opposite of the row, so the criterion cannot be said to cover it even
  generously. The reference's route carries `Policy = FirstTimeSetupOrIgnoreParentalControl` and
  declares the `403`. Breaches Principle VIII, and — if the row is accurate — Principle I, since
  Atrium would be answering an account the reference refuses.

  **The row carries no citation**, which is why this cannot be fixed by reading. Measure whether
  the reference actually refuses; if it does, this is a missing check and a criterion, and if it
  does not, the row has been wrong since 001 was written and eleven features and three audits
  passed over it.

  > **Measured 2026-09-04 by C1, and the box stays unticked on purpose. It refuses.**
  > `tools/probe_system_info_permission.py` stood up a single-use instance of the pinned version
  > and read the whole 2x2 — caller on the LAN or off it, against an account holding the flag or
  > not `[probe: tools/probe_system_info_permission.py, Jellyfin 10.11.11, 2026-09-04]`.
  >
  > **The row is right that a `403` exists and wrong about what reaches it, and the correction
  > makes H1 larger rather than smaller.** There is no permission on this route.
  > `FirstTimeSetupOrIgnoreParentalControl` is `FirstTimeSetupRequirement(validateParentalSchedule:
  > false, requireAdmin: false)`, whose handler succeeds for both roles the reference issues a
  > token; the branch that would refuse needs the `Guest` role, and the authentication handler
  > gives that role to nobody. The refusal comes from the default handler underneath the policy, it
  > is `EnableRemoteAccess`, and it only fires from an address the server does not count as local —
  > proved rather than assumed, by `/System/Endpoint`'s own `IsInNetwork` reading `true` before the
  > instance's LAN was narrowed and `false` after. An account with **every** `Enable*` flag off is
  > answered `200` from the LAN. Remote and without the flag it is `403`, empty body, no content
  > type; remote **with** the flag, `200`. `/Users/Me` is refused identically, and so is an
  > **administrator** who lacks the flag, because the check runs ahead of the administrator bypass.
  >
  > **So closing this is not a check inside `get_system_info`.** It is a server-wide gate ahead of
  > every route that takes a token, plus a definition of "this server's own network" that v1 does
  > not have, landing in an already-`Implemented` feature — a Principle I scope call this project's
  > owner reserves, which is why C1 stopped here rather than implementing it. What C1 landed is the
  > probe, the citation the row never had, and the correction to the two documents the measurement
  > falsified: 001 §3.2's row now states the condition and says why no criterion accompanies it,
  > 002 §3.5's *"the unenforced flags all gate features v1 does not have"* now records the one that
  > does not, and behaviours §5's policy-flags row is narrowed to say that this delta is observable
  > rather than not. **What is owed is the decision**: implement the gate, or accept the divergence
  > deliberately in §4 and say what it costs. Ticking this box needs one of the two.

- [ ] **H2 · `specs/010-conformance-harness/plan.md:416-424`** — §5 names a class that does not
  exist, in a module that does not hold it.
  > ```
  > @dataclass(frozen=True)
  > class Case:                 # one row of request-cases.yaml, resolved
  >     endpoint: str           # the surface.yaml path
  >     method: str
  >     name: str
  >     query: Mapping[str, str]
  >     body: object | None
  >     anchors: Mapping[str, "Anchor"]  # path parameter -> how to resolve it per server (§6.1.1)
  >     identities: tuple[str, ...]      # the roles it is meaningful for; empty means all
  > ```

  The record is `RequestCase` at `tools/_allowlist.py:349`, not `Case` in `tools/differential.py`.
  Five of the eight lines are false: `name` is `id`; `query` and `body` are raw strings, not
  `Mapping[str, str]` and `object | None`; `anchors` is a `Tuple[Anchor, ...]`, not a mapping keyed
  by parameter; `method` is a derived property, not a field. `content_type`, `needs` and
  `what_it_is_for` are absent, though §4.3 records all three as T6 additions.

  **The `body` line is the sharpest, because the contract's type is one the feature deliberately
  cannot use.** The code's own docstring says the register's `body-binding-dollar-message` row is a
  case whose body is not JSON, and a field parsed as JSON at load could not hold it. Breaches
  Principle III; §5 was amended eight times for other findings, so this block was passed over
  rather than abstracted.

- [ ] **H3 · `specs/009-playlists/plan.md:378`** — §5 states an invariant T12 broke, and the same
  plan's §6.6 describes the break.
  > "**Every read takes a `User` and there is no variant that does not.** That is the invariant this
  > feature is most likely to lose: a helper added later that reads entries "just for the count" is
  > how §3.17's divergence stops applying to one route."

  `by_id_for_deletion` (`src/atrium/db/repositories.py:1309`) takes no `User`, and its own docstring
  calls it "the third read, and the only one that takes no reader". §5's block lists eight methods
  where the class has nine, and `tests/unit/test_playlist_repository.py:425-433` classifies methods
  into **three** sets — `READS`, `WRITES`, `UNFILTERED_READS` — not the two §5 promises. §6.6 at
  line 673 describes the third read correctly, so the plan contradicts itself.

  Breaches Principle III. It is high rather than medium because §9 of that plan nominates this
  invariant as the feature's likeliest loss: a false statement of the thing a document warns you to
  watch is worse than silence, since it tells a reader the watch has been kept.

## Medium

### The criterion gap — M1 to M10, one class

Each of these is a `spec.md` §3.x behaviour row with an implementation **and** a full test file,
but no acceptance criterion in §5 — so `tests/conformance/test_acceptance.py` can never name those
tests and they can be weakened without any criterion failing. All breach Principle VIII in its
accounting half. The resolution in every case is a criterion, not code.

- [ ] **M1 · `specs/001-server-identity-and-discovery/spec.md:146-151`** — `/System/Info`'s value
  table. AC-5 covers status codes and "superset agreeing on shared fields", none of the values.
  `test_system_info_claims_no_capability_it_lacks` and `test_system_info_omits_the_null_property`
  exist and are in no acceptance-map row.
- [ ] **M2 · `specs/002-authentication-users-and-sessions/spec.md:117-124`** — the identification
  header's grammar table. §6 claims "the grammar table (AC-3)", but AC-3 is about the five token
  mechanisms and its mapped tests contain no grammar test.
  `test_the_grammar_matches_the_reference_row_for_row` and its neighbours are unmapped.
- [ ] **M3 · `specs/004-metadata-resolution/spec.md:217`** — the Local-only refresh mode.
  > "| **Local only** | Sidecars, tags and local artwork; no network |"

  AC-10 covers Replace's lock half and AC-11 Default; no criterion mentions Local-only. Four
  dedicated tests, none mapped.
- [ ] **M4 · `specs/005-item-query-api/spec.md:344`** — `GET /Items/Filters`. AC-1 names it only to
  *exempt* it from the envelope and never says what it returns. Five tests, none mapped.
- [ ] **M5 · `specs/005-item-query-api/spec.md:174-188`** — the wide-widths table, added
  2026-09-03. AC-18 reaches the full-body half generously; nothing covers the view-row half or the
  list-row-carries-none half. The whole of `tests/conformance/test_item_wide_widths.py` is unmapped.
- [ ] **M6 · `specs/005-item-query-api/spec.md:146-161`** — `MediaSources`' per-account flags,
  amended 2026-09-02. 008's AC-31 covers the negotiation routes only, not 005's listings. All
  thirteen tests in `tests/conformance/test_item_media_source_policy.py` are unmapped.
- [ ] **M7 · `specs/008-playback-negotiation-and-delivery/spec.md:855-860`** — the segment route's
  six refusal rows, **and AC-32 miscounts because of them**.
  > "| No credential | `401`, empty body |"

  AC-32 says `/universal` and the two playlist routes "are the three delivery routes that refuse
  without a token"; §3.7 makes four. The acceptance map hangs
  `test_the_segment_route_requires_a_token` off AC-32 — a criterion whose text excludes that route.
  T10's amendment extended AC-32 for the playlists; T11 added the segment table and extended
  nothing.
- [ ] **M8 · `specs/008-playback-negotiation-and-delivery/spec.md:231-232`** — the negotiation's own
  two refusal rows, on an L3 route. The table's other two rows have AC-31 and AC-5; these have
  nothing.
- [ ] **M9 · `specs/009-playlists/spec.md:136-137`** — two of the creation table's refusals. AC-2
  and AC-3 cover the `Name` and `Ids`-resolution refusals only, and §6 says "the two `400` shapes"
  where T8 counted four.
- [ ] **M10 · `specs/011-subtitle-delivery/spec.md:609`** — the format-spelling table's `subrip` and
  `webvtt` rows.
  > "| `subrip`, `webvtt` | **200** | `application/octet-stream` |"

  A format with no media type answering a body under the fallback label, and a *different* body
  than its canonical spelling. AC-13 covers §3.7's rows only.

### The rest

- [ ] **M11 · `specs/004-metadata-resolution/plan.md:264`** — §5's call to the orchestrator raises
  `TypeError` as written.
  > "`refresh.refresh_library(library, mode, providers, session)` is the orchestrator and the only
  > caller of `apply`."

  The real signature (`src/atrium/metadata/refresh.py:184-192`) is
  `refresh_library(library, session, *, mode, tags, roots, providers)`: `session` is second, not
  fourth, and `mode` and `providers` are keyword-only. This is the entry point the plan hands to
  `library/scan.py`, so argument order is transcription-level detail, not shape. Principle III.
- [ ] **M12 · `specs/011-subtitle-delivery/plan.md:365`** — §5 names an argument `streams`; it is
  files, and the code says why it cannot be streams.
  > "**`MediaProbeRepository`** gains `put_external(library_id, relative_path, streams)`"

  It is `files: Sequence[DiscoveredSubtitles]` (`src/atrium/db/repositories.py:1950`), whose type
  opens "**A file rather than a stream, because the change signal belongs to the file.**" This is
  the exemplar's own class — a contract naming something the code deliberately is not — and 011's
  `tasks.md:369` **already records it in a Done note** without folding it back into §5. Principle
  III's "it goes back into the spec, in the same change".
- [ ] **M13 · `specs/002-authentication-users-and-sessions/spec.md:222`** — a `prior-probe` on a
  claim that was discharged two days before this audit.
  > "`[prior-probe: Jellyfin 10.11.11, 2026-06-13]`"

  `docs/compatibility/reference-target.md`'s register struck this row as discharged 2026-09-02 by
  `tools/probe_public_users.py`, and behaviours §2.2 was upgraded to a plain `[probe:]`. This copy
  was not, so the register's own sentence — "Every discharged citation is now a plain `probe:` and
  its row is struck" — is false of it. A prior-probe reads as an undischarged debt. Principle II.
- [ ] **M14 · `specs/001-server-identity-and-discovery/plan.md:176-179`** — a claim carried as
  unverified after it was measured, and measured the other way.
  > "The base does **not** set `exclude_none`: absent-versus-null is per property in the
  > reference and currently unverified
  > ([behaviours §1.7](../compatibility/behaviours.md#17-a-null-property-is-absent-everywhere-by-one-setting)),
  > so it is decided per model against a golden response rather than by a blanket rule."

  The link resolves to a section that now contradicts the sentence citing it: §1.7 is titled "A null
  property is absent, everywhere, by one setting" and carries both a `[source:]` and a `[probe:]`.
  The code went the other way too — `src/atrium/compat/model.py:255-282` drops every null globally
  with a `NULL_KEPT` exception set, and its docstring says so explicitly. Principles II and III.
- [ ] **M15 · `AGENTS.md:98,110`** — the project's own map says a thing exists and does not exist,
  twelve lines apart.
  > "**010's plan was drafted and accepted on 2026-09-01**, so its task list is the next gate: the
  > plan's bulk is the thing that does not exist yet — a **single-use reference instance** …"

  010 is `Implemented`, its fifteen tasks ran on 2026-09-02, and `tools/_reference.py` and
  `tools/reference_instance.py` both exist — as the same paragraph says further down. Breaches
  AGENTS.md's own "The state is in the files, not here". A reader who finishes the paragraph is not
  misled; one who stops at "the next gate" is.
- [ ] **M16 · `specs/002-authentication-users-and-sessions/plan.md:64-89`** — §3's tree omits two
  modules 002's own tasks created: `src/atrium/domain/session.py` (T5, `fb4df84`) and
  `src/atrium/logs.py` (T14, `54fd132`). Neither name appears in the file; `atrium.logs` is in §6
  prose and the risk table only. A finding rather than accepted drift because **001's plan
  reconciles its own tree explicitly** — "Six of those modules are not in the tree this plan was
  accepted with, and one that was is gone" — and 002's does not. Principle III.
- [ ] **M17 · `src/atrium/api/media_info.py:681`** — a guard the whole suite survives losing.
  > ```
  > if probes[index] is not None:
  >     continue
  > ```

  **Verified by mutation on a scratchpad copy: with these two lines deleted the full suite — ~5250
  tests, ffmpeg batteries included — exits 0.** Its deletion re-opens and re-stores every
  already-probed part on every negotiation that fires the trigger, invisibly. The trigger beside it
  *is* held alone. Breaches the project's C4 standard that a test fails when the guard is deleted,
  and `specs/012-negotiation-inputs/tasks.md:929-930` states the opposite: "a later change that
  removes either one alone will still pass this test and the one after it will not" — the green run
  contradicts the second clause for this guard. Resolution: one test asserting the prober is
  invoked zero times for a part that already carries an inspection.
- [ ] **M18 · `tests/unit/test_filters_and_search_routes.py:90-92`** — an assertion whose only
  content-bearing half cannot fail.
  > ```
  > assert body["Years"] == sorted(body["Years"]) == [1900 + 89, *range(1991, 1990 + RATED)] or (
  >     body["Years"] == sorted(body["Years"])
  > )
  > ```

  `assert (sorted AND exact-list) or (sorted)` reduces to `assert sorted`: a wrong `Years` list
  passes as long as it is sorted, while the line reads as though the list were asserted. Breaches
  the project's own standard that a test which would pass against the bug is not a test. Named in
  no acceptance-map row, so no criterion is orphaned by it.

## Low

- [ ] **L1 · `specs/012-negotiation-inputs/tasks.md:22,93,113,998,1043`** — the repository's only
  unresolved anchor, five times.
  > "`[plan §6.8](plan.md#68-what-this-plan-read-and-did-not-measure)`"

  The heading is now "### 6.8 What this plan read, and what T1 measured". T1 renamed the section
  when it turned "did not measure" into "measured" and the five links were not followed.
- [ ] **L2 · `specs/008-playback-negotiation-and-delivery/plan.md:89`** — `src/atrium/domain/media.py`
  is drawn in no plan's §3 tree, though 008 created it and names it twice in prose, and the
  module's docstring points back at the §4 that does not draw it.
- [ ] **L3 · three historical records written in the present tense** —
  `specs/003-library-configuration-and-scanning/tasks.md:527` ("which currently has none", where
  `pyproject.toml:66` now carries `pyyaml>=6.0`), `:754` ("the `MetadataSource` protocol 004 **will**
  implement", where 004 is `Implemented`), and
  `specs/010-conformance-harness/tasks.md:1102` ("each currently saying that no instance was stood
  up", which T9 filled). Each sits inside a completed task's own record, where the present tense
  arguably means "at the time of this task" — but each reads as false today and none carries a date
  to say otherwise.
- [ ] **L4 · `docs/compatibility/reference-target.md:41` and
  `docs/decisions/0007-a-container-runtime-for-the-reference-instance.md:105`** — a probe citation
  naming endpoints rather than a script.
  > "`[probe: /Plugins and /api-docs/openapi.json, Jellyfin 10.11.11, 2026-09-01]`"

  Nothing re-runnable resolves from it — the same class the 2026-08-28 audit closed by folding
  manual measurements into their owning probes.
- [ ] **L5 · `specs/005-item-query-api/spec.md:205`** — an observable reference behaviour with no
  citation in its paragraph.
  > "**The single-item route is the exception to that**, and it is not a choice: the reference emits
  > everything there whatever the request says"

  The claim *is* measured — `tools/probe_item_shapes.py`'s docstring records it — so this is A1
  hygiene, not a false claim.
- [ ] **L6 · `specs/005-item-query-api/spec.md:251`** — the same, borderline.
  > "*Reject with `400`* — the reference answers `200`, so this is itself a delta"

  `[spec: GetItems]` at line 218 declares the 86 parameters and arguably covers acceptance;
  reported under the rule for borderline cases.
- [ ] **L7 · `specs/008-playback-negotiation-and-delivery/plan.md:243`** — `sub_protocol: str | None
  # "hls" | "http"`, where the code is `str | int | None` since 012 T9. Recorded in 012's plan, not
  in 008's, which is the block a reader consults for that dataclass.
- [ ] **L8 · `.../plan.md:229`** — `codec: str`, where the code is `str | None` and the COPY branch
  really can carry `None`. The same block annotates `width`/`height`/`bitrate` as optional, so the
  distinction reads as deliberate.
- [ ] **L9 · `.../plan.md:239-247`** — `Decision` omits `target`, added at 008 T5 (`f8893a6`) and
  named in no plan's text; 008's amendment log skips T5 entirely. (`subtitles` and `subtitle_index`
  are declared in 011's §5, so those two are a pointer rather than a gap.)
- [ ] **L10 · `.../plan.md:318`** — `segment(...)` omits `length_ticks: int = 0`, added at 008 T13;
  the T13 amendment discusses `actualSegmentLengthTicks` in §6.7 without updating the signature.
- [ ] **L11 · `.../plan.md:300-304`** — `master_playlist(...)` omits
  `subtitles: Sequence[AnnouncedSubtitle] = ()`. Declared in 011's §5, and 008's block was
  rewritten by T15 "against the three signatures as they actually stand", so it is behind again.
- [ ] **L12 · `.../plan.md:363-365`** — the comment describes variants that do not exist.
  > "# RangeAnswer is one of: Full(200), Partial(206, start, end), Unsatisfiable(416)"

  `RangeAnswer` is one dataclass with `status`/`start`/`length`/`total` — no variants, and `length`
  rather than `end`. The function's own signature matches exactly.
- [ ] **L13 · `specs/007-user-data-and-playstate/plan.md:174-187`** — `PlayingNow`'s thirteen fields
  are the right thirteen in the wrong order against `src/atrium/users/playing.py:76-96`.
- [ ] **L14 · `specs/010-conformance-harness/plan.md:445-452,428-431`** — `RunReport` grew four
  defaulted fields and `Seat` two after T12; purely additive, every planned call still valid.
- [ ] **L15 · `tests/conformance/test_acceptance.py:890-892`** — 008's AC-30 says a `PlaySessionId`
  "is accepted by the delivery route **and** by `ActiveEncodings`"; its single named test asserts
  the `ActiveEncodings` half only. The delivery half is exercised by an unmapped test with a
  different purpose, so it can be renamed away silently — 010 T15's AC-7 shape.
- [ ] **L16 · `tests/conformance/test_golden_items.py:17-23`** — an anchor no test reads.
  > "every property a golden carries or omits is checkable against that table"

  `grep -rn reference-item-shapes tests/ tools/` finds no reader, so the sixteen `Items.*` goldens'
  only *executed* comparison is Atrium against its own `--update-golden` output; the reference
  anchor bites at human review only. Reported with the caveat that audit 2026-08-28's M47 accepted
  review-plus-anchor as the mechanism.

---

## Clean

**A2** every probe name resolves and every `[source:]` reads `@ v10.11.11` (four of 012's new ones
spot-checked against a local checkout at that tag). **A3** no fabricated citation — no
`prior-probe` was added anywhere in the 012 window, and the one suspicious reuse was chased to a
same-night session. **A4** no live `⚠️ UNVERIFIED` in any of the twelve `Implemented` specs. **A5**
all 91 behaviours entries carry their three fields. **A6** no citation names anywhere
unverifiable. **B4** all 36 frontmatter fields, 24 gate fields and 36 status-table cells agree, and
AGENTS.md's opening agrees with them — the check today's T11 was likeliest to break. **B5** every
`[x]` task in all twelve `tasks.md` files carries its Done note. **C3** every `xfail` is
`strict=True`, the ffmpeg skip is CI-installed, `needs_reference` is pinned to one test. **D1**
012's zero-delta promise held exactly: `surface.yaml`'s last commit predates every 012 merge, and
the 59 served routes reconcile with the file both ways. **D2, D3** every wire-shape move in 012
carries a probe or source citation; every divergence carries its argument. **E1–E4** SPDX complete,
English throughout, no secret or local path, and all 60 recent first-parent commits are merges of
reviewed pull requests.

## Not run

A1 paragraph-by-paragraph over the `plan.md`/`tasks.md` prose of 001–009 (pattern sweep only; those
files were covered by the audits of 2026-08-30 and 2026-09-01). Exhaustive C5 over all ~180
criteria of 001–011 — 012 was read row by row, the rest sampled at every single-test row. Live
probes and the differential, which need a server this audit may not touch. Mutation testing of
guards other than M17.
